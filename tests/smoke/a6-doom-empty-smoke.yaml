# A6: Empty Doom Lore Response (Stubbed API)
# Verifies: WireMock returns [] → empty state view appears
appId: io.appcision.project-doombot
---
- clearState
- launchApp
- tapOn:
    id: "login_username_field"
- inputText: "admin"
- tapOn:
    id: "login_submit_button"
- extendedWaitUntil:
    visible:
      id: "doom_empty_state"
    timeout: 15000
- assertVisible:
    id: "doom_empty_state"
